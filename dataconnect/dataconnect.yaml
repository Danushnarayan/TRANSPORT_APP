specVersion: "v1"
serviceId: "rectransport"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rectransport-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
